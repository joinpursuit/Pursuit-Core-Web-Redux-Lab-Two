import React from "react";

const ToDoComponent = () => {
  return (
    <ul>
      items
      {/* {newToDo.map((item) => {
                return <li key={item.id}>{item.name}</li>
            })} */}
    </ul>
  );
};

export default ToDoComponent;
